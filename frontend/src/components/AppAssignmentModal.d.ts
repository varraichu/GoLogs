type Props = {
    groupId: string;
    groupName: string;
    onClose: () => void;
};
export default function AppAssignmentModal({ groupId, groupName, onClose }: Props): import("preact").JSX.Element;
export {};
